# 불균형 처리 비교 실험 실행 안내

이번 파일은 `molding_imbalance_colab.ipynb`다. 이전 DNN 전용 파일 대신 이 파일을 사용한다. 모델 학습은 아직 실행하지 않았다.

## 실행 순서

1. Colab에서 파일 → 노트북 업로드로 ipynb를 연다. Drive에 사본을 저장한다.
2. CPU 기본 런타임이면 충분하다. TensorFlow나 imbalanced-learn을 설치하지 않는다.
3. 첫 코드 셀의 `DATA_DIR`를 `labeled_data.csv`가 있는 Drive 폴더로 수정한다. 따옴표를 유지하고 CSV 파일명이 아닌 폴더 경로를 넣는다. 제공한 가공 CSV들도 같은 폴더에 있으면 파일 관계 점검을 함께 수행한다.
4. 1~4번 셀을 실행해 데이터 경로·라벨·분리가 통과하는지 확인한다. 이 단계는 모델을 학습하지 않는다.
5. 5번 셀에서 RF 4조건×3검증 구간, 총 12회 학습한다. 진행마다 모델명·구간·AP·경과 시간이 나온다.
6. 6번 셀에서 검증 임계값을 고정하고 기준 모델과 선택 후보를 최종 학습한다. 최대 2회다.
7. 7번 셀에서 동일한 11월 데이터의 지표와 제품별 미탐·오탐·검사량을 확인한다.
8. 8번 셀에서 `imbalance_results.zip`을 다운로드한다. 실행 결과가 저장된 ipynb도 Colab 파일 메뉴에서 다운로드한다.

`imbalance_results.zip`과 실행한 ipynb를 전달하면 결과를 분석한다. 지금은 개선 결과가 없으며, 이 파일을 준비한 것과 성능이 개선된 것은 구분한다.

결과는 Drive의 `molding_imbalance_results/run_실행시각/`에 저장한다. 같은 셀을 반복 실행하면 재학습하므로 완료 후 불필요하게 다시 실행하지 않는다.

## 무엇이 다른가

- 가상 라벨 22회 반복 대신 실제 라벨이 있는 원본으로 제한된 RF 실험을 한다.
- 원본의 제품명(LH/RH)을 유지한다. 이는 기존 시간순 RF에도 들어 있던 정보이며 이번에 새로 발명한 특성이 아니다. 제공 moldset 기반 DNN에서는 이 제품명 정보가 빠져 있었다.
- 전처리를 원래 학습 구간으로 맞춘 후, 학습 불량만 복제한다. 평가 불량은 복제하지 않는다.
- 기준 RF도 새 데이터 분리와 같은 임계값 규칙으로 다시 학습해 비교한다. 기존 AP .1195와 바로 빼서 개선율을 계산하지 않는다.
- 이미 확인한 11월 데이터를 재사용한 후속 탐색이다. 검증에서 선택한 모델을 11월 지표에 맞춰 다시 바꾸지 않는다.

## 데이터 점검에서 확인한 사실

원본 정제 후 분석 대상은 5,230건·불량 60건이다. `moldset_labeled.csv` 2,607개 ID는 모두 원본에 있고 라벨도 일치하므로 파일을 합쳐도 새로운 독립 기록을 확보하는 것이 아니다.

가공 RG3 파일의 불량 25건 모두 같은 24개 공정값을 가진 양품 기록이 있다. 원본에서는 전체 불량 60건 중 44건이 동일한 공정값의 양품 기록을 가지지만, 제품명을 함께 키로 사용하면 이 충돌은 0건이다. 이 결과는 입력 표현에서 제품 구분의 필요성을 보여줄 뿐, 모든 불량이 예측 가능하거나 라벨이 오류라는 뜻은 아니다.

11월 이전 RG3 LH 학습 불량은 0건이고 11월 평가에는 5건이 있다. 오버샘플링은 존재하지 않는 그 불량 패턴을 새로 만들어내지 못한다. 이 한계를 결과 해석에 유지한다.

## 검증 범위

작성 측에서는 실제 데이터 로딩·라벨·ID 대조·시간 분리·생산 지시 비중복, 복제 대상이 학습 불량에 한정되는지, 임계값 동점으로 검사 상한을 넘지 않는지 확인했다. 모델 학습 함수 호출은 차단하고 검사했다. Colab의 실제 모델 실행과 성능 검증은 사용자가 수행한 결과로 확인한다.
